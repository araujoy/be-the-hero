//JSX - (js+xml) quando html está dentro do js
//Componente: função do react que retorna html (podendo ter js, css)
//Propriedade: similar aos atributos do html, porém mais poderosos. O 'title' do header é um exemplo.

import React from 'react';

import Routes from './routes';

function App() {
  return (
      <Routes/>
  );
}

export default App;
